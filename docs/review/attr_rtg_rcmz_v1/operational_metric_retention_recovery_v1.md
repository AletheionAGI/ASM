# ATTR-RTG-RCMZ-V1 operational metric-retention recovery

Status: AUTHORIZED OPERATIONAL RECOVERY — NO TRAINING

The completed run produced 20 update-2000 checkpoints and immutable official scalar rows SHA-256 `d0b7f2611528ffbeb6b150e6f98f4d8c68818fd2582c51a81f30bcd755d69902`. A summarization integration defect discarded otherwise finite metrics when one `safe_service` fold had no eligible denominator.

This amendment authorizes only:

1. independent per-metric hierarchical folding;
2. `safe_service=null` and cell `INVALID` whenever its denominator is absent;
3. retention of finite H8 NLL, ECE-15, unsafe-selection, coverage and abstention;
4. endpoint-wise contrast computation, with any missing endpoint and every aggregate gate failing closed;
5. score-only recovery from the exact 20 completed checkpoints;
6. regeneration of the registered PNG/SVG charts, HTML and manifests.

It forbids training, optimizer steps, checkpoint writes or replacement, scalar imputation, denominator relaxation, gate rescue, protocol/data/model/config changes, or silent overwrite of the prior release. Recovery writes to a fresh output directory.
